title: Multiple ARP Requests from a Single MAC Address
status: experimental
correlation:
    type: value_count
    rules:
        - arp_event
    group_by:
        - source.mac
    timespan: 1m
    condition:
        gte: 20
        field: destination.ip
tags:
  - attack.correlation
